// IF/ELSE STATEMENTS

var firstName = 'John';
var civilStatus = 'single';

if (civilStatus === 'married') {
    console.log(firstName + ' is married.')
} else {
    console.log('Hopefully ' + firstName + ' will marry soon.')
}

var isMarried = true;
if (isMarried) {
    console.log(firstName + ' is married.')
} else {
    console.log('Hopefully ' + firstName + ' will marry soon.')
}
